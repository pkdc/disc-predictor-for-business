version: '3.8'

services:
  app:
    build:
      context: .
    container_name: disc-pred-mvp
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - XLA_PYTHON_CLIENT_MEM_FRACTION=0.3
    restart: unless-stopped